{
  "global": {
    "check_for_updates_on_startup": true,
    "show_in_menu_bar": true
  },
  "profiles": [
    {
      "name": "Default",
      "selected": true,
      "complex_modifications": {
        "parameters": {
          "basic.to_if_held_down_threshold_milliseconds": 120,
          "basic.to_if_alone_timeout_milliseconds": 200
        },
        "rules": [
          {
            "description": "Map the Globe/Fn key on Mac's built-in keyboard to F13 while preserving Fn+F1–F12 behavior",
            "manipulators": [
              {
                "type": "basic",
                "from": {
                  "key_code": "fn",
                  "modifiers": { "optional": ["any"] }
                },

                "to_if_held_down": [
                  { "key_code": "f13" }
                ],

                "to_if_alone": [ /* when pressed */
                  { "key_code": "f13" }
                ],

                "to": [ /* when pressed with another key */
                  { "key_code": "fn" }
                ]
              }
            ]
          }
        ]
      },
      "devices": [],
      "simple_modifications": [],
      "fn_function_keys": []
    }
  ]
}
