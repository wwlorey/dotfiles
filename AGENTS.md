# Dotfiles Repository

Configuration rules for this repo live in the `config` skill (`.agents/skills/config/SKILL.md`). Consult it before editing any config file, creating or modifying a skill, or changing anything mirrored from `$HOME`.
