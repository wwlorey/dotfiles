#!/bin/bash

fileType=$(file --mime-type -b "$1")

# Open PDF files with 'open' command, open other files with text editor
if [[ $fileType == "application/pdf" ]]; then
    echo "here1"
    echo "$1"
    open "$1"
else
    echo "here1"
    echo "$1"
    echo '$EDITOR'
    $EDITOR "$1"
fi
