#!/bin/bash

if ! [[ $2 ]]
then
  echo "Performs an audio-only download of the given video. (Usage: download-yt <filename minus the extension> <video URL>)"
  exit 1
fi

FILENAME="$1"
URL="$2"

yt-dlp --ignore-errors --extract-audio -v --format bestaudio/best -i "$URL" --output "$FILENAME.%(ext)s"
